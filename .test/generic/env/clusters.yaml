clouds:
  aws:
    domain: eks.otomi.cloud
    clusters:
      demo:
        enabled: false
  google:
    domain: gke.otomi.cloud
    clusters:
      demo:
        enabled: true
  azure:
    domain: aks.otomi.cloud
    clusters:
      demo:
        enabled: false
  onprem:
    domain: onprem.otomi.cloud
    clusters:
      demo:
        enabled: false
