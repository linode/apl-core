charts:
  # $id: '#/properties/charts'
  additionalProperties: true
  default: {}
  description: An explanation about the purpose of this instance.
  properties:
    cert-manager:
      $ref: 'charts.cert-manager.yaml#/cert-manager'
    drone:
      $ref: 'charts.drone.yaml#/drone'
    external-dns:
      $ref: 'charts.external-dns.yaml#/external-dns'
    gatekeeper-operator:
      $ref: 'charts.gatekeeper-operator.yaml#/gatekeeper-operator'
    harbor:
      $ref: 'charts.harbor.yaml#/harbor'
    istio:
      $ref: 'charts.istio.yaml#/istio'
    keycloak:
      $ref: 'charts.keycloak.yaml#/keycloak'
    loki:
      $ref: 'charts.loki.yaml#/loki'
    oauth2-proxy:
      $ref: 'charts.oauth2-proxy.yaml#/oauth2-proxy'
    otomi-api:
      $ref: 'charts.otomi-api.yaml#/otomi-api'
    otomi-web:
      $ref: 'charts.otomi-web.yaml#/otomi-web'
    prometheus-operator:
      $ref: 'charts.prometheus-operator.yaml#/prometheus-operator'
    redis-shared:
      $ref: 'charts.redis-shared.yaml#/redis-shared'
    sitespeed:
      $ref: 'charts.sitespeed.yaml#/sitespeed'
  required:
    - cert-manager
    - drone
    - external-dns
    - gatekeeper-operator
    - harbor
    - istio
    - keycloak
    - loki
    - oauth2-proxy
    - otomi-api
    - otomi-web
    - prometheus-operator
    - redis-shared
    - sitespeed
  title: The charts schema
  type: object
