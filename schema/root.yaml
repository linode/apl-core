$id: 'root'
$schema: http://json-schema.org/draft-07/schema
additionalProperties: true
description: The root schema comprises the entire JSON document.
properties:
  charts:
    $ref: 'charts.yaml#/charts'
  clouds:
    $ref: 'clouds.yaml#/clouds'
  cluster:
    $ref: 'cluster.yaml#/cluster'
  enabled:
    $ref: 'enabled.yaml#/enabled'
  google:
    $ref: 'google.yaml#/google'
  k8s:
    $ref: 'k8s.yaml#/k8s'
  oidc:
    $ref: 'oidc.yaml#/oidc'
  proxies:
    $ref: 'proxies.yaml#/proxies'
  pullSecret:
    $ref: 'pullSecret.yaml#/pullSecret'
  redkubesSlackUrl:
    $ref: 'redkubesSlackUrl.yaml#/redkubesSlackUrl'
  services:
    $ref: 'services.yaml#/services'
  sites:
    $ref: 'sites.yaml#/sites'
  teamConfig:
    $ref: 'teamConfig.yaml#/teamConfig'
  tenant:
    $ref: 'tenant.yaml#/tenant'
  toolsVersion:
    $ref: 'toolsVersion.yaml#/toolsVersion'
required:
  - charts
  - clouds
  - cluster
  - enabled
  - google
  - k8s
  - oidc
  - proxies
  - pullSecret
  - redkubesSlackUrl
  - services
  - sites
  - teamConfig
  - tenant
  - toolsVersion
title: The root schema
type: object
