- name: blackbox
  additionalLabels:
    prometheus: po-prometheus
  namespaceSelector:
    matchNames:
      - monitoring
  selector:
    matchLabels:
      app: prometheus-blackbox-exporter
  endpoints:
    - port: http
      interval: 30s
      path: /metrics
