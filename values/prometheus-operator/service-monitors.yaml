- name: blackbox
  additionalLabels:
    prometheus: po-prometheus
  namespaceSelector:
    matchNames:
      - monitoring
  selector:
    matchLabels:
      app: prom-blackbox-exporter
  endpoints:
    - port: http
      interval: 30s
      path: /metrics
