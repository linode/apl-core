groups:
  - name: sites.rules
    rules:
      - alert: ProbeFailing
        expr: probe_success{job="probe-services"} == 0
        for: 10m
        labels:
          severity: critical
        annotations:
          description: 'Could not reach {{ $labels.instance }} during last 10 minutes'
          summary: '{{ $labels.instance }} is down'
