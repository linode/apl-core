{{- $v := .Environment.Values }}
{{- $httpbin := $v | get "charts.hello" dict }}

nameOverride: hello
fullnameOverride: hello

image:
  tag: latest

resources:
  requests:
    cpu: 10m
    memory: 8Mi
  limits:
    cpu: 100m
    memory: 64Mi

# podSecurityContext:   
#   seLinuxOptions:
#     - level: s3:c223,c456
#       role: object_w
#       type: svirt_sandbox_file_w
#       user: system_admin

podAnnotations:
  policies.otomi.io/ignore: "pspallowedusers,bannedimagetags"