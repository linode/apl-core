apiVersion: templates.gatekeeper.sh/v1beta1
kind: ConstraintTemplate
metadata:
  name: k8sbannedimagetags
spec:
  crd:
    spec:
      names:
        kind: K8sBannedImageTags
      validation:
        # Schema for the `parameters` field
        openAPIV3Schema:
          properties:
            tags:
              type: array
              items:
                type: string
  targets:
    - target: admission.k8s.gatekeeper.sh
      libs:
        - |-
          {{- readFile "../../policies/lib/core.rego" | nindent 10 }}
      rego: |-
        {{- readFile "../../policies/k8sbannedimagetags.rego"  |  nindent 8  }}
