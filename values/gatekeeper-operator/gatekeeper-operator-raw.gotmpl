{{- $v := .Environment.Values }}
{{- $constraints := (exec "bash" (list "-c" "find  ./constraint-templates -name '*.yaml'")) | splitList "\n" }}
resources:
{{- range $constraint := $constraints }}{{ if ne $constraint "" }}
  - {{ tpl (readFile $constraint) . | nindent 4  }}
{{- end }}{{ end }}
