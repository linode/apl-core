{{ $v := .Values }}
{{- $domain := printf "%s%s.%s" $v.otomi.teamPrefix "admin" $v.cluster.domainSuffix }}
global:
  hosts:
    domain: {{ $domain }}
    https: true

{{- if eq $v.cluster.provider "google" }}
nodeSelector:
  cloud.google.com/gke-nodepool: nodes
{{- end }}
  