{{- $v := .Values }}
{{- $vt := $v.apps.vault }}
watchNamespace: "vault"

# @TODO:
image:
  tag: {{ $vt.image.tag }}
{{- with $v._files | get "otomi.globalPullSecret" nil }}
global:
  imagePullSecrets:
    - otomi-pullsecret-global
{{- end }}

podSecurityContext:
  runAsUser: 65534
  runAsGroup: 65534
  fsGroup: 1337
