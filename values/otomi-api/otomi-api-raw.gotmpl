{{- $v := .Environment.Values }}
{{- $o := index $v.charts "otomi-api" }}

resources:
  - apiVersion: v1
    data:
      .dockerconfigjson: {{ $o.pullSecret.data }}
    kind: Secret
    metadata:
      name: {{ $o.pullSecret.secretName }}
    type: kubernetes.io/dockerconfigjson
