{{- $v := .Environment.Values }}
{{- $o := index $v.charts "otomi-api" }}

image:
  tag: {{ $o.image.tag }}

imagePullSecrets:
  - name: {{ $o.pullSecret.secretName }}

git:
  repoUrl: {{ $o.git.repoUrl }}
  localPath: "/tmp/otomi-stack"
  user: {{ $o.git.user }}
  email: {{ $o.git.email }}
  password: {{ $o.git.password }}
