resources:
    requests:
        cpu: 50m
        memory: 32Mi
    limits:
        cpu: 100m
        memory: 64Mi
deployment:
  secretName: external-dns
  secretKey: secret
nodeSelector: {{- toYaml .Values.otomi.nodeSelector | nindent 2 }}
