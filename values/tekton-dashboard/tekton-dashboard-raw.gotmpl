{{- $v := .Values }}
{{- $httpRoute := tpl (readFile "../../helmfile.d/snippets/routes.gotmpl") $v | fromYaml }}
{{- $domain := $v.cluster.domainSuffix }}
{{- $hostname := print "tekton." $domain }}
resources:
  - apiVersion: gateway.networking.k8s.io/v1
    kind: HTTPRoute
    metadata:
      {{ with $httpRoute.annotations }}
      annotations:
        {{ . | toYaml | nindent 8 }}
      {{- end }}
      name: tekton-dashboard
    spec:
      hostnames:
        - {{ $hostname }}
      parentRefs:
        {{- $httpRoute.parentRefs | toYaml | nindent 8 }}
      rules:
        - backendRefs:
            - group: ""
              kind: Service
              name: tekton-dashboard
              port: 9097
          matches:
            - path:
                type: PathPrefix
                value: /
        {{- $httpRoute.authRules | toYaml | nindent 8 }}
  - {{ tpl (readFile "../../helmfile.d/snippets/authpolicy-oauth2-ext.gotmpl") (dict "prefix" "tekton" "gatewayName" $v.ingress.platformClass.className "host" $hostname) | nindent 4 }}
  - {{ tpl (readFile "../../helmfile.d/snippets/serviceentry.gotmpl") (dict "name" "tekton" "host" $hostname) | nindent 4 }}
