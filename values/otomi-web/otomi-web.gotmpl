{{- $v := .Environment.Values }}
{{- $o := index $v.charts "otomi-web" }}
{{- $domain := printf "otomi.%s" $v.cluster.domain }}

replicaCount: 2

image:
  registry: eu.gcr.io
  repository: otomi-cloud/otomi-stack-web
  tag: {{ $o.image.tag }}
  pullPolicy: {{ $o.image | get "pullPolicy" "IfNotPresent" }}

env:
  API_BASE_URL: /api
  CONTEXT_PATH: ''
