tabWidth: 2
semi: false
trailingComma: all
singleQuote: true
printWidth: 120
overrides:
  - files:
      - 'profiles/**/*.yaml'
      - 'tests/fixtures/**/*.yaml'
    options:
      tabWidth: 4
      useTabs: true
