# Initializes the installation status. If missing, apl-operator assumes the installation to be completed.
apiVersion: v1
kind: ConfigMap
metadata:
  name: apl-installation-status
  namespace: apl-operator
data:
  status: "pending"
