apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "otomi-deps.fullname" . }}-test-connection"
  labels:
    {{- include "otomi-deps.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "otomi-deps.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
