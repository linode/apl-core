apiVersion: v1
kind: Service
metadata:
  name: {{ include "otomi-deps.fullname" . }}
  labels:
    {{- include "otomi-deps.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "otomi-deps.selectorLabels" . | nindent 4 }}
