apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "otomi-keycloak-operator.fullname" . }}-test-connection"
  labels:
    {{- include "otomi-keycloak-operator.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "otomi-keycloak-operator.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
