{{- $v := .Values }}
apiVersion: operators.coreos.com/v1
kind: OperatorGroup
metadata:
  name: argocd-operator
spec:
  targetNamespaces:
  - {{ .Release.Namespace }}
  {{- range $teamId, $_ := $v.teamConfig }}
  - team-{{ $teamId }}
  {{- end }}
