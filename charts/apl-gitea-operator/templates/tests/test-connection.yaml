apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "apl-gitea-operator.fullname" . }}-test-connection"
  labels:
    {{- include "apl-gitea-operator.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "apl-gitea-operator.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
