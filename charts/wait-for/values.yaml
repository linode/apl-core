# the url of the service:
url: ''

# the host of the service (used as header)
host: ''

# retries defaults to 50 with backoff strategy of eventually 30 secs max:
# retries: 100

# optional custom CA to use:
# customCa: |

# optional pull secrets to use:
imagePullSecrets: []

# otomi/tasks image to use, defaults to the one from .versions file
# tasksVersion: master

serviceAccount:
  create: true
# podDnsPolicy: "None"
# podDnsConfig:
#   nameservers:
#     - "1.1.1.1"
#     - "8.8.8.8"