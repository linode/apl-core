apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "gitea-app-operator.fullname" . }}-test-connection"
  labels:
    {{- include "gitea-app-operator.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "gitea-app-operator.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
