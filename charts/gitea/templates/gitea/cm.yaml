
{{- if eq .Values.certManagerStage "staging" }}
apiVersion: v1
kind: ConfigMap
metadata:
  labels: {{- include "gitea.labels" $ | nindent 4 }}
  name: letsencrypt-staging-ca
data:
  letsencrypt-staging.pem: |
    {{- .Values.letsencryptCA | nindent 4 }}
---
{{- end }}
