storage:
  trace:
    block:
      version: vParquet4
      dedicated_columns:
        - name: cluster
          type: string
          scope: resource
        - name: service.name
          type: string
          scope: resource
