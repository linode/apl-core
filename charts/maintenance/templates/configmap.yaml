{{- $v := .Values }}

{{ range $name, $spec := $v.tasks }}
{{ if and $spec.enabled $spec.configmap }}
---
apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ $name }}
  labels: {{- include "maintenance.labels" $ | nindent 4 }}
data:
{{ toYaml $spec.configmap | indent 2 }}
{{ end }}
{{ end }}
