{{- $v := .Values }}

{{ range $v.jobs }}
{{ if and .enabled .configmap }}
---
apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ .name }}
  labels: {{- include "maintenance.labels" $ | nindent 4 }}
data:
{{ toYaml .configmap | indent 2 }}
{{ end }}
{{ end }}
