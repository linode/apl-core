apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRoleBinding
metadata:
  name: opa-exporter-binding
roleRef:
  apiGroup: rbac.authorization.k8s.io
  kind: ClusterRole
  name: opa-exporter
subjects:
- kind: {{ .Values.serviceAccount.name }}
  name: default
  namespace: {{ .Release.Namespace }}