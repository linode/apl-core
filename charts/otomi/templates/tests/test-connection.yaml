apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "otomi.fullname" . }}-test-connection"
  labels:
{{ include "otomi.labels" . | indent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args:  ['{{ include "otomi.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
