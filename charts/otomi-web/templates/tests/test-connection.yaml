apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "otomi-web.fullname" . }}-test-connection"
  labels:
{{ include "otomi-web.labels" . | indent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args:  ['{{ include "otomi-web.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
