{{- $isDistributed := eq (include "loki.deployment.isDistributed" .) "true" -}}
{{- if $isDistributed }}
{{- include "loki.pdb" (dict "target" "query-frontend" "component" .Values.queryFrontend "ctx" .) }}
{{- end }}
