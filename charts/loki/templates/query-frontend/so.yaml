{{- $isDistributed := eq (include "loki.deployment.isDistributed" .) "true" -}}
{{- if $isDistributed }}
{{- include "loki.keda" (dict "target" "query-frontend" "component" .Values.queryFrontend "ctx" . "kind" "Deployment") }}
{{- end }}
