{{- $isDistributed := eq (include "loki.deployment.isDistributed" .) "true" -}}
{{- if $isDistributed -}}
{{- include "loki.serviceAccount" (dict "target" "query-frontend" "component" .Values.queryFrontend "ctx" .) }}
{{- end -}}
