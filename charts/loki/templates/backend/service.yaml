{{- $isSimpleScalable := eq (include "loki.deployment.isScalable" .) "true" -}}
{{- if $isSimpleScalable }}
{{- include "loki.service" (dict "target" "backend" "component" .Values.backend "ctx" .) }}
{{- end -}}
