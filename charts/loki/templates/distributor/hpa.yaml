{{- $isDistributed := eq (include "loki.deployment.isDistributed" .) "true" -}}
{{- if $isDistributed }}
{{- include "loki.hpa" (dict "target" "distributor" "component" .Values.distributor "ctx" . "kind" "Deployment") }}
{{- end }}
