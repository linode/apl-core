{{- $isDistributed := eq (include "loki.deployment.isDistributed" .) "true" -}}
{{- if $isDistributed -}}
{{- include "loki.serviceAccount" (dict "target" "distributor" "component" .Values.distributor "ctx" .) }}
{{- end -}}
