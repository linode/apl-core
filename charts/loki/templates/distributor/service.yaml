{{- $isDistributed := eq (include "loki.deployment.isDistributed" .) "true" -}}
{{- if $isDistributed -}}
{{- include "loki.service" (dict "target" "distributor" "component" .Values.distributor "ctx" .) }}
{{- end -}}
