{{- $isDistributed := eq (include "loki.deployment.isDistributed" .) "true" -}}
{{- if $isDistributed }}
{{- include "loki.serviceAccount" (dict "target" "index-gateway" "component" .Values.indexGateway "ctx" .) }}
{{- end -}}
