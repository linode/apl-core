{{- $isDistributed := eq (include "loki.deployment.isDistributed" .) "true" -}}
{{- if $isDistributed }}
{{- include "loki.pdb" (dict "target" "index-gateway" "component" .Values.indexGateway "ctx" .) }}
{{- end }}
