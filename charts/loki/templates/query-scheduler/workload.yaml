{{- $isDistributed := eq (include "loki.deployment.isDistributed" .) "true" -}}
{{- if $isDistributed }}
{{- include "loki.component" (dict "ctx" . "component" .Values.queryScheduler "target" "query-scheduler") }}
{{- end }}
