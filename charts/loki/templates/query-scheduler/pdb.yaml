{{- $isDistributed := eq (include "loki.deployment.isDistributed" .) "true" -}}
{{- if $isDistributed }}
{{- include "loki.pdb" (dict "target" "query-scheduler" "component" .Values.queryScheduler "ctx" .) }}
{{- end }}
