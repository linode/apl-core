{{- $isSimpleScalable := eq (include "loki.deployment.isScalable" .) "true" -}}
{{- if $isSimpleScalable }}
{{- include "loki.serviceAccount" (dict "target" "read" "component" .Values.read "ctx" .) }}
{{- end -}}
