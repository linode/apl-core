{{- $isDistributed := eq (include "loki.deployment.isDistributed" .) "true" -}}
{{- if (and $isDistributed (gt (int .Values.bloomPlanner.replicas) 0)) -}}
{{- include "loki.component" (dict "ctx" . "component" .Values.bloomPlanner "target" "bloom-planner") }}
{{- end }}
