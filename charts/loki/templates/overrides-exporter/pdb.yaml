{{- $isDistributed := eq (include "loki.deployment.isDistributed" .) "true" -}}
{{- if and $isDistributed .Values.overridesExporter.enabled }}
{{- include "loki.pdb" (dict "target" "overrides-exporter" "component" .Values.overridesExporter "ctx" .) }}
{{- end }}
