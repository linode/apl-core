{{- $isDistributed := eq (include "loki.deployment.isDistributed" .) "true" -}}
{{- if $isDistributed }}
{{- include "loki.serviceAccount" (dict "target" "compactor" "component" .Values.compactor "ctx" .) }}
{{- end -}}
