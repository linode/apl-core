{{- $isSimpleScalable := eq (include "loki.deployment.isScalable" .) "true" -}}
{{- if $isSimpleScalable }}
{{- include "loki.serviceAccount" (dict "target" "write" "component" .Values.write "ctx" .) }}
{{- end -}}
