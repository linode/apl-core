{{- $v := .Values }}
{{- range $spec := $v.jobs }}
{{- if default true $spec.enabled }}
apiVersion: v1
kind: ConfigMap
metadata:
  name: job-{{ $spec.name }}
  labels: {{- include "jobs.labels" $ | nindent 4 }}
data: 
  {{- with $spec.env  }}
    {{- toYaml . | nindent 4 }}
  {{- end }}
  {{- if hasKey $spec "init" }}
  {{- with $spec.init.env  }}
    {{- toYaml . | nindent 4 }}
  {{- end }}
  {{- end }}
---
{{- end }}
{{- end }}
