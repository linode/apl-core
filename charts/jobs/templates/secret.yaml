
{{- $v := .Values }}
{{- if and (default true $v.enabled) $v.secret }}
apiVersion: v1
kind: Secret
metadata:
  labels: {{- include "jobs.labels" $ | nindent 4 }}
  name: job-{{ $v.name }}
data:
{{- range $key, $val := $v.secret }}
  {{ $key }}: "{{ $val | b64enc }}"
{{- end }}
---
{{- end }}
