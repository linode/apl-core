backoffLimit: 3
enabled: true
env: {}
  # receiver: world

files: {}
  # someFile: someData
  # someFile2: someData2
  # somefile3: |-
  #     some data on another line
  #     another line

image: {}
  # repository: busybox
  # tag: latest
  # pullPolicy: IfNotPresent

init: {}
  # env: {}
  # image:
  #     repository: busybox
  #     tag: latest
  #     pullPolicy: IfNotPresent
  # script: echo "bla"
  # secrets:
  # - name: secret-name
  #   entries:
  #       - foo
  #       - bar

resources:
  limits:
    cpu: 200m
    memory: 256Mi
  requests:
    cpu: 100m
    memory: 128Mi

# runPolicy is only used if type: Job
runPolicy: Always # OnSpecChange
# schedule: '1 * * * *'

script: echo "bla"
secrets: []
  # - name: secret-name
  #   entries:
  #       - foo
  #       - bar

type: Job # CronJob
ttlSecondsAfterFinished: 86400

# Policies
{{- $addIgnorePolicyAnnotation := readFile "../helmfile.d/utils/addIgnorePolicyAnnotation.gotmpl" }}
annotations: {{- tpl $addIgnorePolicyAnnotation (dict "policies" (list "banned-image-tags")) | nindent 6 }}
podSecurityContext:
  runAsUser: 1000
