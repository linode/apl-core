## chart values for testing

These dirs contain example values.yaml files to pass into the chart to verify they are valid.

e.g. 

* [config/values.yaml](config/values.yaml) verifies custom config in the `ConfigMap`


### To run them 

```bash 
make verify
```