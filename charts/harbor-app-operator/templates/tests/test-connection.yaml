apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "harbor-app-operator.fullname" . }}-test-connection"
  labels:
    {{- include "harbor-app-operator.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "harbor-app-operator.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
