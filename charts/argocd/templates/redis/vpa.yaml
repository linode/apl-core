{{- if and (.Values.redis.vpa) (.Values.redis.vpa.enabled) }}
apiVersion: autoscaling.k8s.io/v1
kind: VerticalPodAutoscaler
metadata:
  name: {{ include "argo-cd.redis.fullname" . }}
  namespace: {{ include  "argo-cd.namespace" . }}
  labels:
    {{- include "argo-cd.labels" (dict "context" . "component" .Values.redis.name "name" .Values.redis.name) | nindent 4 }}
    {{- with .Values.redis.vpa.labels }}
      {{- toYaml . | nindent 4 }}
    {{- end }}
  {{- with .Values.redis.vpa.annotations }}
  annotations:
  {{- toYaml . | nindent 4 }}
  {{- end }}
spec:
  targetRef:
    apiVersion: "apps/v1"
    kind: Deployment
    name: {{ template "argo-cd.redis.fullname" . }}
  updatePolicy:
    updateMode: {{ .Values.redis.vpa.updateMode | quote }}
  resourcePolicy:
    containerPolicies:
    - containerName: {{ .Values.redis.name }}
    {{ with .Values.redis.vpa.containerPolicy }}
    {{- toYaml . | nindent 6 }}
    {{- end }}
{{- end }}
