{{- if and (.Values.server.vpa) (.Values.server.vpa.enabled) }}
apiVersion: autoscaling.k8s.io/v1
kind: VerticalPodAutoscaler
metadata:
  name: {{ include "argo-cd.server.fullname" . }}
  namespace: {{ include  "argo-cd.namespace" . }}
  labels:
    {{- include "argo-cd.labels" (dict "context" . "component" .Values.server.name "name" .Values.server.name) | nindent 4 }}
    {{- with .Values.server.vpa.labels }}
      {{- toYaml . | nindent 4 }}
    {{- end }}
  {{- with .Values.server.vpa.annotations }}
  annotations:
  {{- toYaml . | nindent 4 }}
  {{- end }}
spec:
  targetRef:
    apiVersion: "apps/v1"
    kind: Deployment
    name: {{ template "argo-cd.server.fullname" . }}
  updatePolicy:
    updateMode: {{ .Values.server.vpa.updateMode | quote }}
  resourcePolicy:
    containerPolicies:
    - containerName: {{ .Values.server.name }}
    {{ with .Values.server.vpa.containerPolicy }}
    {{- toYaml . | nindent 6 }}
    {{- end }}
{{- end }}
