image:
  repository: bitnami/kubectl
  tag: 1.21.8
# The certificate directory to store the ca. Is dependent on distro/image, so check with provider.
# It is not needed to provide this as we do introspection to determine context, but some providers might need customization.
# certDir: /usr/local/share/ca-certificates
# The custom CA root to install on the nodes
customCa: |
# The url of the private registry
registryUrl: ''
# The command to run after copying the cert
updateCmd: ''
priorityClassName: ''