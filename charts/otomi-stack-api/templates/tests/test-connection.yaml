apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "otomi-stack-api.fullname" . }}-test-connection"
  labels:
{{ include "otomi-stack-api.labels" . | indent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args:  ['{{ include "otomi-stack-api.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
