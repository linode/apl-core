{{- if .Values.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ template "otomi-stack-api.serviceAccountName" . }}
  labels:
{{ include "otomi-stack-api.labels" . | indent 4 }}
{{- end -}}
