apiVersion: config.gatekeeper.sh/v1alpha1
kind: Config
metadata:
  name: config
  namespace: gatekeeper-system
spec:
  sync:
    syncOnly:
      - version: v1
        kind: Namespace
      - version: v1
        kind: Pod
      - version: v1
        kind: Service
