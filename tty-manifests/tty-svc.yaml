apiVersion: v1
kind: Service
metadata:
  labels:
    app: tty-${SUB}-${TARGET_TEAM}
  name: tty-${SUB}-${TARGET_TEAM}
  namespace: team-admin
spec:
  ports:
    - name: 8080-8080
      port: 8080
      protocol: TCP
      targetPort: 8080
  selector:
    app: tty-${SUB}-${TARGET_TEAM}
  type: ClusterIP
---

