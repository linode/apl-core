{{ printf "%s-%s" .cluster.provider .cluster.name }}:
  values:
    - {{- . | toYaml | nindent 6 }}
    - ../env/env/default.yaml
    - ../env/env/{{ .cluster.name }}.yaml
    - ../env/env/{{ .cluster.provider }}/default.yaml
    - ../env/env/{{ .cluster.provider }}/{{ .cluster.name }}.yaml
    - ../core.yaml
