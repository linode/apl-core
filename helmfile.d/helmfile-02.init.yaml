bases:
  - snippets/defaults.yaml

{{ readFile "snippets/templates-skeleton.gotmpl" }}

releases:
  - name: prometheus-operator-init
    installed: true
    namespace: default
    labels:
      init: true
    <<: *skeleton
