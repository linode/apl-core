bases:
  - snippets/defaults.gotmpl

{{ readFile "snippets/templates.gotmpl" }}

releases:
  - name: istio-operator
    installed: true
    namespace: default
    labels:
      pkg: istio-operator
    <<: *default
  - name: keycloak
    installed: true
    namespace: keycloak
    <<: *default
