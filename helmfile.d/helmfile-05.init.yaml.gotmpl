bases:
  - snippets/defaults.yaml
---
bases:
  - snippets/defaults.gotmpl
---
bases:
  - snippets/env.gotmpl
---
bases:
  - snippets/derived.gotmpl
---
{{ readFile "snippets/templates.gotmpl" }}
{{- $v := .Values }}
{{- $a := $v.apps }}

releases:
  - name: argocd-artifacts
    installed: {{ $a | get "argocd.enabled" }}
    namespace: argocd
    labels:
      pkg: argocd
      app: core
    <<: *raw
  - name: argocd-image-updater
    installed: {{ $a | get "argocd.enabled" }}
    namespace: argocd
    labels:
      pkg: argocd-image-updater
    <<: *default
  - name: dex-artifacts
    installed: {{ $a | get "dex.enabled" }}
    namespace: dex
    labels:
      pkg: dex
    <<: *raw
  - name: dex
    installed: {{ $a | get "dex.enabled" }}
    namespace: dex
    labels:
      pkg: dex
    <<: *default
  - name: keycloak-artifacts
    installed: {{ $a | get "keycloak.enabled" }}
    namespace: keycloak
    labels:
      pkg: keycloak
    <<: *raw
  - name: keycloak
    installed: {{ $a | get "keycloak.enabled" }}
    namespace: keycloak
    labels:
      pkg: keycloak
    <<: *default
