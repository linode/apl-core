bases:
  - snippets/defaults.gotmpl
---
{{ readFile "snippets/templates.gotmpl" }}
{{- $v := .Environment.Values }}
{{- $c := $v.charts }}
{{- $knativeVersion := print "0." ((min (int ($v.cluster.k8sVersion | replace "1." "")) 18 ) | toString) }}

releases:
  - name: jobs-artifacts
    installed: true
    namespace: maintenance
    labels:
      tag: jobs
    <<: *raw
  - name: knative-init
    installed: true
    namespace: default
    labels:
      init: true
      pkg: knative
    chart: /tmp/charts/knative-init-{{ $knativeVersion }}
    hooks:
      - events: ["prepare"]
        showlogs: false
        command: "../bin/skeleton-chart.sh"
        args: ["knative-init-{{ $knativeVersion }}"]
  - name: loki-artifacts
    installed: true
    namespace: monitoring
    labels:
      pkg: loki
    <<: *raw
  - name: gatekeeper-operator
    installed: {{ $c | get "gatekeeper-operator.enabled" false }}
    namespace: default
    labels:
      pkg: gatekeeper-operator
    <<: *default
