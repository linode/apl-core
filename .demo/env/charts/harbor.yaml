charts:
    harbor:
        _rawValues: {}
        enabled: true
        persistence:
            imageChartStorage:
                type: gcs
                gcs:
                    bucket: otomi-demo-harbor
                    rootdirectory: /demo
