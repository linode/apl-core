charts:
    harbor:
        enabled: true
        persistence:
            imageChartStorage:
                type: gcs
                gcs:
                    bucket: otomi-harbor
                    rootdirectory: /google/demo
