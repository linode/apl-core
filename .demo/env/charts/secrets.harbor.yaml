charts:
    harbor:
        adminPassword: someAdminPassword
        secretKey: someSecretKey
        persistence:
            imageChartStorage:
                gcs:
                    encodedkey: someEncodedkey
