apps:
    gitea:
        _rawValues:
            init:
                resources:
                    requests:
                        cpu: 10m
                        memory: 10Mi
            memcached:
                resources:
                    requests:
                        cpu: 10m
                        memory: 10Mi
            postgresql:
                resources:
                    requests:
                        cpu: 10m
                        memory: 10Mi
            resources:
                requests:
                    cpu: 10m
                    memory: 10Mi
