apps:
    gatekeeper:
        _rawValues:
            audit:
                resources:
                    requests:
                        cpu: 10m
                        memory: 10Mi
            controllerManager:
                resources:
                    requests:
                        cpu: 10m
                        memory: 10Mi
