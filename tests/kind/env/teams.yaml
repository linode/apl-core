teamConfig:
    admin:
        id: admin
    demo:
        id: demo
        limitRange:
            default:
                cpu: 100m
                memory: 128Mi
            defaultRequest:
                cpu: 50m
                memory: 64Mi
        resourceQuota:
            services.loadbalancers: '1'
