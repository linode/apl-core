apiVersion: bitnami.com/v1alpha1
kind: SealedSecret
metadata:
  annotations:
    sealedsecrets.bitnami.com/namespace-wide: "true"
  name: keycloak-secrets
  namespace: apl-secrets
spec:
  encryptedData:
    idp_clientSecret: somsecretvalue
  template:
    immutable: false
    metadata:
      name: keycloak-secrets
      namespace: apl-secrets
    type: kubernetes.io/opaque
