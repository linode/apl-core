apiVersion: bitnami.com/v1alpha1
kind: SealedSecret
metadata:
  annotations:
    sealedsecrets.bitnami.com/namespace-wide: "true"
  name: argocd-secrets
  namespace: apl-secrets
spec:
  encryptedData:
    redisPassword: someEncryptedPassword
  template:
    immutable: false
    metadata:
      name: argocd-secrets
      namespace: apl-secrets
    type: kubernetes.io/opaque
