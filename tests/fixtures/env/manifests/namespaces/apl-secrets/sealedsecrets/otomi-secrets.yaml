apiVersion: bitnami.com/v1alpha1
kind: SealedSecret
metadata:
  annotations:
    sealedsecrets.bitnami.com/namespace-wide: "true"
  name: otomi-secrets
  namespace: apl-secrets
spec:
  encryptedData:
    adminPassword: bladibla
    git_password: gitPasswordForTesting
    globalPullSecret_password: blablabla
  template:
    immutable: false
    metadata:
      name: otomi-secrets
      namespace: apl-secrets
    type: kubernetes.io/opaque
