apiVersion: bitnami.com/v1alpha1
kind: SealedSecret
metadata:
  annotations:
    sealedsecrets.bitnami.com/namespace-wide: "true"
  name: team-admin-settings-secrets
  namespace: apl-secrets
spec:
  encryptedData:
    password: YTrnkdUsKPcGATfg
  template:
    immutable: false
    metadata:
      name: team-admin-settings-secrets
      namespace: apl-secrets
    type: kubernetes.io/opaque
