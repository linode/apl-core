apiVersion: bitnami.com/v1alpha1
kind: SealedSecret
metadata:
  annotations:
    sealedsecrets.bitnami.com/namespace-wide: "true"
  name: team-demo-settings-secrets
  namespace: apl-secrets
spec:
  encryptedData:
    password: somesecretvalue
  template:
    immutable: false
    metadata:
      name: team-demo-settings-secrets
      namespace: apl-secrets
    type: kubernetes.io/opaque
