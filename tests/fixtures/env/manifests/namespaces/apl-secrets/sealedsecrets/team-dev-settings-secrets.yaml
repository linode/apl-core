apiVersion: bitnami.com/v1alpha1
kind: SealedSecret
metadata:
  annotations:
    sealedsecrets.bitnami.com/namespace-wide: "true"
  name: team-dev-settings-secrets
  namespace: apl-secrets
spec:
  encryptedData:
    password: IkdUsKPcGAdanjas
  template:
    immutable: false
    metadata:
      name: team-dev-settings-secrets
      namespace: apl-secrets
    type: kubernetes.io/opaque
