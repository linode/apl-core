apiVersion: bitnami.com/v1alpha1
kind: SealedSecret
metadata:
  annotations:
    sealedsecrets.bitnami.com/namespace-wide: "true"
  name: prometheus-secrets
  namespace: apl-secrets
spec:
  encryptedData:
    remoteWrite_rwConfig_basicAuth_password: blalalalalal
  template:
    immutable: false
    metadata:
      name: prometheus-secrets
      namespace: apl-secrets
    type: kubernetes.io/opaque
