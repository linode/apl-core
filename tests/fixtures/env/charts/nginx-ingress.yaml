charts:
    nginx-ingress:
        _rawValues: {}
        maxBodySize: 1024m
        resources:
            limits:
                cpu: 200m
                memory: 256Mi
            requests:
                cpu: 100m
                memory: 192Mi
        private:
            enabled: true
            resources:
                limits:
                    cpu: 200m
                    memory: 256Mi
                requests:
                    cpu: 100m
                    memory: 192Mi
            service:
                annotations:
                    private: 'true'
