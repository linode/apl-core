apps:
    loki:
        enabled: true
        persistence:
            size: 20Gi
        retention:
            duration: 24h
            period: 24h
        v11StartDate: '2021-05-13'
        storage:
            storageType: azure
            azure:
                account_name: account_name
                container_name: container_name
        resources:
            ingester:
                requests:
                    cpu: 100m
                    memory: 128Mi
                limits:
                    cpu: 900m
                    memory: 512Mi
            gateway:
                requests:
                    cpu: 100m
                    memory: 128Mi
                limits:
                    cpu: 900m
                    memory: 512Mi
            querier:
                requests:
                    cpu: 100m
                    memory: 128Mi
                limits:
                    cpu: 900m
                    memory: 512Mi
            distributor:
                requests:
                    cpu: 100m
                    memory: 128Mi
                limits:
                    cpu: 900m
                    memory: 512Mi
            queryFrontend:
                requests:
                    cpu: 100m
                    memory: 128Mi
                limits:
                    cpu: 900m
                    memory: 512Mi
