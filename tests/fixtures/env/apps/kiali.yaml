apps:
    kiali:
        enabled: true
        resources:
            pod:
                requests:
                    cpu: 50m
                    memory: 128Mi
                limits:
                    cpu: 100m
                    memory: 256Mi
            operator:
                requests:
                    cpu: 50m
                    memory: 256Mi
                limits:
                    cpu: 200m
                    memory: 1Gi
