kind: AplApp
metadata:
    name: kubeflow-pipelines
spec:
    _rawValues:
        mysql:
            storage:
                storageClass: linode-block-storage-retain
    enabled: true
    persistence:
        mysql:
            size: 20Gi
