apps:
    rabbitmq-cluster-operator:
        enabled: true
        resources:
            operator:
                limits:
                    cpu: 900m
                    memory: 512Mi
                requests:
                    cpu: 100m
                    memory: 128Mi
