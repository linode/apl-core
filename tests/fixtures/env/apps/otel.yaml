apps:
    otel:
        enabled: true
        mode: deployment
        resources:
            collector:
                limits:
                    cpu: 900m
                    memory: 512Mi
                requests:
                    cpu: 100m
                    memory: 128Mi
