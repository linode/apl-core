apps:
    velero:
        cloud:
            provider:
                # azure:
                #     aadClientSecret: somesecret
                google:
                    saKeyJson: '{key in json format}'
