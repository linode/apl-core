apps:
    keycloak:
        enabled: true
        idp:
            alias: redkubes-azure
            clientID: otomi
        theme: otomi
        resources:
            keycloak:
                limits:
                    cpu: '200m'
                    memory: '512Mi'
                requests:
                    cpu: '100m'
                    memory: '256Mi'
            operator:
                limits:
                    cpu: '200m'
                    memory: '512Mi'
                requests:
                    cpu: '100m'
                    memory: '256Mi'
