apps:
    harbor:
        enabled: true
        backup:
            enabled: false
        persistence:
            imageChartStorage:
                gcs:
                    bucket: otomi-harbor
                    rootdirectory: /google/demo
                type: gcs
