apps:
    harbor:
        enabled: true
        persistence:
            imageChartStorage:
                gcs:
                    bucket: otomi-harbor
                    rootdirectory: /google/demo
                type: gcs
        core: {}
        jobservice:
            storageClassName: fast
        registry:
            credentials: {}
            storageClassName: fast
        # redis:
        #     storageClassName: fast
        chartmuseum:
            storageClassName: fast
        trivy:
            storageClassName: fast
