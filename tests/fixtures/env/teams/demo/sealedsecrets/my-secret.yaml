apiVersion: bitnami.com/v1alpha1
kind: SealedSecret
metadata:
    name: my-secret
spec:
    encryptedData:
        password: encrypted-password-value-text
    template:
        metadata:
            name: my-secret
        type: kubernetes.io/opaque
