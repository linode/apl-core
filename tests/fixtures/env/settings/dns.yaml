kind: AplDns
metadata:
    name: dns
spec:
    domainFilters:
        - otomi.cloud
    policy: upsert-only
    provider:
        # cloudflare: {}
        linode: {}
        # akamai:
        #     host: akamai.com
    zoneIdFilters: []
