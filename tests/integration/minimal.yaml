# Minimal values file with defaults
cluster:
  name: CLUSTER_NAME_PLACEHOLDER
  k8sContext: CONTEXT_PLACEHOLDER
otomi:
  version: 'OTOMI_VERSION_PLACEHOLDER'
apps:
  keycloak:
    rawValues:
      image:
        registry: mirror.registry.linodelke.net/docker/bitnami/keycloak
        repository: docker/bitnami/keycloak
        tag: 26.3.2-debian-12-r0
