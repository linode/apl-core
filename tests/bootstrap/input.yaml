# Minimal values file with defaults
cluster:
  k8sContext: CONTEXT_PLACEHOLDER
  domainSuffix: my.localhost
otomi:
  version: 'main'
apps:
  metrics-server:
    enabled: false
kms:
  sops:
    provider: age
    age:
      keys: ''
      privateKey: ''
