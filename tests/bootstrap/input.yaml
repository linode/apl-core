# Minimal values file with defaults
cluster:
  name: 'dev'
  provider: custom
  k8sContext: CONTEXT_PLACEHOLDER
  domainSuffix: my.localhost
otomi:
  version: 'main'
apps:
  metrics-server:
    enabled: false
